import 'package:flutter/material.dart';

class ProjectEditSubmit extends StatelessWidget {
  const ProjectEditSubmit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
